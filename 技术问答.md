# PAEG 技术问答文档（路演答辩准备 · 55+ 问）

> 版本：v0.53 | 更新：2026-08-12 | 用途：路演/答辩 Q&A 速查
> 组织：按主题分 14 章，每条含【问题】【参考答案】。答案基于真实代码/版本/实测数据。

---

## 一、项目定位与差异化

### Q1: PAEG 和 ChatGPT/豆包/Kimi 的教育模式有什么本质区别？
**A**: 通用 AI 教育产品本质是"让 LLM 回答问题"——一次性输出答案，没有教学法、没有过程、没有评估、没有陪伴。PAEG 用 **Agent 架构把"教学"从 LLM 的一次性输出中结构化抽离**：诊断→计划→呈现→评估→调整→反思六阶段闭环，让智能体扮演教师、LLM 扮演教师手中的笔。教育的价值在于"教的过程"而非答案本身，这是 PAEG 与所有问答式 AI 的根本分野。

### Q2: 为什么不直接用现有 Agent 框架（LangChain/LlamaIndex）？
**A**: 我们**借鉴**了成熟框架的方法论（LlamaIndex 的 query() 包装、LangChain 的工具注册、Kraken 的大型单体分层），但 PAEG 的核心是**教育领域的领域建模**——9 个教学 subagent、17 维学生画像、学段-学科联动、薇依教育哲学，这些是通用框架不提供的。通用框架擅长"接 API"，PAEG 擅长"教学生"。

### Q3: 和"刷题 AI"（作业帮/猿辅导类）的差异？
**A**: 刷题 AI 只做"才"（知识传授），PAEG 做到"德才兼备"——因材施教（Individuality 17 维画像）+ 立德树人（AffectionSupportor 情绪陪伴）。学生说"我撑不住了"时，刷题 AI 继续讲下一题，PAEG 会停下来陪伴。这是价值观壁垒，无法通过堆算力复制。

### Q4: 市场规模和付费意愿怎么论证？
**A**: Khanmigo（可汗学院 AI，月费 4-9 美元）、Duolingo Max（月费 30 美元）已验证教育 AI 付费意愿。家长/学生的核心痛点是"没有懂我的老师"——个性化、有陪伴、能记住学生情况。博雅教育（人文/哲学/批判性思维）是通用 AI 的空白，正是 PAEG 的垂直定位。

### Q5: PAEG 是 To C 还是 To B 产品？
**A**: 双轮驱动——To C 直接服务 K12/成人学习者（个性化陪伴教学）；To B 可作为学校的 AI 教学助手或机构的白标解决方案（模块化架构支持定制）。当前原型已验证单用户完整闭环，多用户 SQLite 扩展已就绪（v0.38）。

---

## 二、整体架构

### Q6: 请描述 PAEG 的整体架构。
**A**: 六层架构：用户层（学生/外部智能体）→ 应用层（Flask Server + 意图路由 + 学段联动）→ 主 Agent（Émile + 9 个 subagent + 35 学科）→ LLM 层（DeepSeek）→ 工具+MCP 层（工具链 + 10 技能 + 3 个 MCP server）→ 本地资源层（知识库/画像/记忆/产出物）。三层扩展：检索增强（RRF）、视频生成（演讲稿驱动）、PPT 讲义（经验库驱动）。

### Q7: 9 个 subagent 分别负责什么？为什么这样拆分？
**A**: Diagnostor（诊断基础）→ Planner（计划路径）→ Presenter（讲解）→ Evaluator（评估掌握）→ Adapter（调整方法）→ AnswerSolver（直接要答案）→ AffectionSupportor（情绪陪伴）→ SelfUpdateAgent（自我更新）→ Individuality（个体化建模）。设计原则：**LLM 只做最擅长的事**——诊断深度、评估分数、调整决策用确定性规则（可测试可复现），只有"生成讲解"用 LLM。

### Q8: 为什么评估用确定性启发式而不是 LLM？
**A**: 可复现、可测试、可审计。LLM 评分会漂移（同题两次不同分），确定性启发式保证同一回答永远得到同一分数——这对教育评估是硬要求（学生和家长需要稳定反馈）。LLM 负责的是"讲解"这类生成任务，评估这类判定任务交给规则。

### Q9: 多用户扩展是怎么做的？
**A**: SQLite + JSON 文件落盘（v0.38）——每个用户独立 users_data/profile.json 画像 + 会话持久化（Thread/Turn/Item 三层模型，借鉴 Codex，支持 fork/archive/SSE 续传）。注册即对齐（users.json 与画像双源一致）。

### Q10: 前后端怎么通信？
**A**: Flask + SSE 流式（Server-Sent Events）——逐 token 推送流式响应，前端可显示打字机效果 + 停止生成按钮。REST API 处理常规请求（登录/注册/历史/工具调用）。前端单文件 Web GUI（index.html + KaTeX 数学渲染 + marked Markdown）。

---

## 三、教学闭环细节

### Q11: 一次完整的教学对话流程是怎样的？
**A**: 学生提问 → 危机检查（_affection_gate_check，危机先行）→ 学段检查（grade_blocked?）→ 诊断（Diagnostor 判断基础）→ 计划（Planner 按学科风格+学段定制）→ 呈现（Presenter 分步讲解）→ 评估（Evaluator 双维评估）→ 调整（Adapter 换风格/补例子）→ 反思（平均分<0.7 触发 evolve_prompt）。

### Q12: 六阶段闭环中哪一步最关键？
**A**: 诊断（Diagnostor）——错误诊断会导致后续全部错位。诊断用确定性规则 + 知识掌握状态（双向证据：学生自述 + 实际表现）双重确认，避免单一信号误判。

### Q13: 怎么判断学生"真的学会了"？
**A**: Evaluator 双维评估：知识维度（能否正确复述/应用）+ 过程维度（能否独立推演）。结合知识掌握状态的双向证据（学生说"会了" + 实际答题表现），不轻信单一信号。

### Q14: 教学跨学段怎么处理？
**A**: 学段-学科联动（v0.25）——每个学科绑定最低学段（SUBJECT_MIN_GRADE：初中12/高中22/本科28/考研2）。高中生问语言学 → 智能拦截并提示"该学科需大学本科及以上学段，可切换学段"，而不是误报"未收录"。教学循序渐进，符合教学伦理。

### Q15: 一个学生问的问题超纲（如初中生问大学数学）怎么办？
**A**: 学段拦截（grade_blocked）提示切换学段。如果学生坚持要了解，Adapter 会调整讲解深度——用初中生能懂的语言讲直觉，不堆公式。深度控制由 8 层约束中的 L4/L6 管理（默认深度/扩展深度）。

### Q16: 教学记忆怎么跨会话保持？
**A**: 三层：①Individuality 17 维画像持久化（users_data/profile.json）②教学记忆库（memory/ 每节课的要点/盲点/进展）③SelfUpdateAgent 会话后反思（平均分<0.7 时沉淀改进建议）。每轮对话注入历史 + 画像 + 记忆，实现"记住学生"。

---

## 四、语言规范技术

### Q17: 语言规范三层（L1/L2/L3）具体是什么？
**A**: L1 提示词约束（注入 system prompt 的语法自检 7 项：主谓宾/动宾搭配/介词规范/词法完整/句法完整）；L2 规则检测（零 LLM 成本的 detect_ai_taste AI 味检测 + _check_ellipsis 省略句检测）；L3 LLM 修正（refiner.refine 保持风格地补全重写，用薇依语料矫正）。L0 保底 11 条规则永不跳过。

### Q18: 为什么"换更强的模型 ≠ 语言更规范"？
**A**: 语言规范性是**独立于模型性能的能力**。强模型会生成更多"像 AI"的表达（完整但空洞），而不是更像老师。必须靠语法分析和限制**程序化保证**——L1 约束 + L2 规则检测 + L3 修正，与模型能力解耦。

### Q19: 有哪些实测数据证明语言规范有效？
**A**: 病句捕获率 15/16（94%），正确句零误报（v0.25 语言规范测试报告）；AI 味检测阈值 0.4 触发；实测"我有点倦，想和你探讨"→"我有点疲倦了，但我还是想和你探讨这个问题"（词法完整）。

### Q20: 介词规范具体修什么？
**A**: 介词悬空补主句——"关于这个知识点，要记住重点。"→"关于这个知识点，我们需要记住它的重点。"；"通过学习，掌握了很多方法。"→"通过学习，我掌握了很多学习方法。"（补主语）；"把他帮助了。"→"他帮助了那个学生。"（把字句误用）；"被批评了。"→"他受到了老师的批评。"（被字句补施事）。

### Q21: 8 个生成模块都过语言规范了吗？
**A**: 是（v0.46 审计全绿）——teach 教学（流式/同步）、chat 闲聊、answer 找答案、method 学习方法、knowledge 知识库、affection 倾诉、recommend 推荐、resource summary，加上早退分支（ppt/grade_blocked/unknown）全部经过 L1+L2+L3。新增生成点自动纳入 polish（v0.46.2 起）。

---

## 五、动态提示词约束

### Q22: 8 层约束（v0.50）是什么？和 v0.46 的 3 位掩码什么关系？
**A**: v0.46 是 3 位掩码（A 直接性/B 情绪温度/C 深度详细度），v0.50 升级为 **8 层约束（L0-L7）**：L0 语言底线（永不跳过）/L1 危机安全/L2 直接答案/L3 情绪温度/L4 深度（默认）/L5 过程讲解/L6 扩展深度/L7 迁移创造。3 位掩码 000-111 完整映射 8 层放开组合。

### Q23: 8 层约束怎么工作？
**A**: 6 个模式设置掩码（_set_constraint_flags）→ detect_constraint_flags 检测用户文本中的约束信号（LLM+规则，utils/constraint_signals.py）→ _build_constraint_layers 动态拼装约束层（prompts.py，L0 恒在首位）→ 固定提示词 + 动态约束段注入 system prompt。约束段长度随 mask 变化（默认 1089 字 → 全放开 1836 字）。

### Q24: 用户说"直接给我答案"会发生什么？
**A**: detect_constraint_flags 检测到直接性信号 → 触发 A 组/L2 放开 → 取消"直接给答案/列要点/别铺垫"约束 → LLM 可以直给答案。同时 L0 保底（语言规范/安全伦理）仍严格生效。

### Q25: 用户说"我想深入理解这个理论"会怎样？
**A**: v0.50 后 C 组扩为 6 层并加入正向深度信号词（深入/透彻/讲透/本质/详细/研究级）→ 命中则取消 C 深度约束，允许 LLM 讲深。这是 v0.47 规划、v0.50 落地的修复。

### Q26: 8 层约束和提示词注入防护的关系？
**A**: L0 保底含安全伦理规则，任何 mask 下都不放开——即使全部放宽（mask=111），语言规范/格式/反AI腔核心/安全护栏依然注入。这是"约束栈"的底线设计：**自由是有限的，底线是无限的**。

---

## 六、检索增强

### Q27: 检索增强（v0.45）解决了什么问题？
**A**: 中文检索命中率低（jieba 切词缺失导致"分离定律"被拆碎）、多路结果去重不干净（URL 假重复）、推荐类问题需要真联网。v0.45 落地 RRF 融合 + URL 规范化 + jieba 切词 + 相关性打分 + 多查询并行。

### Q28: RRF 融合是什么？为什么用它？
**A**: Reciprocal Rank Fusion（倒数排名融合，k=60）——跨查询按 rank 累积分数，不依赖归一化，无 score 尺度问题（OpenSearch 默认）。替代简单去重，融合多路检索结果排序稳定。

### Q29: 中英双语变体和多轮改写是什么？
**A**: 检索前生成查询变体：中文原文 + 英文翻译 + 多轮对话上下文改写（5 主题 × 3 轮 → 16 条），提高召回率。多查询 K=2-3 为甜区（4+ 只增延迟无 recall 增益）。

### Q30: 实测效果怎么样？
**A**: "超导体的量子隧穿效应原理与应用"→ 9 条相关结果，3.5s（此前 1-4 条跑偏）；"量子计算 最新突破 2024"→ 5 条相关。URL 规范化实测去 30-60% 假重复。

### Q31: 冷门中文专名（如"西蒙娜·薇依"）检索不到怎么办？
**A**: Bing 会被"西蒙电气/动画角色"污染。根治方案是配置 TAVILY_API_KEY/SERPER_API_KEY/BOCHA_API_KEY（代码 failover 已支持，key 存在时自动优先）或部署 SearXNG 自托管（ChinaSo+Bing+360 引擎）。属部署配置项，非代码缺陷。

---

## 七、PPT 与视频生成

### Q32: PPT 生成是怎么实现的？
**A**: 用户上传文档 + 知识库检索 + 对话历史 → LLM 生成大纲（## 标题 + 要点）→ pptx_mcp_server.py（FastMCP + python-pptx）自动排版（封面+内容页+品牌配色+页码+备注）→ 输出可下载 .pptx（downloads/ppt/）。v0.51-53 升级为授课讲义模式（学习目标/要求/形式/视觉风格）。

### Q33: PPT 为什么会出现错位/重叠？根因是什么？
**A**: 三层根因链（2026-08-11 量化诊断）：①装饰形状数量失控（教学页单页 57 形状，大量空形状压内容）②坐标硬编码无碰撞检测（装饰坐标手写固定值，与动态内容未做相交检查）③内容长度动态变化（文本换行后高度变，装饰坐标不随之调整）。根本是**缺"内容流+装饰层"分离的布局系统**。

### Q34: 怎么预防 PPT 错位？
**A**: 四条铁律：①单页形状 ≤20 ②装饰用底层占位（先画装饰再画内容）③内容元素添加前碰撞检测（相交面积 >30% 则调整）④QA 循环必做 bounding box 相交扫描。经验沉淀在 memo/022。

### Q35: 视频生成（v0.53）和之前有什么不同？
**A**: 从"先做视频再配音"改为**演讲稿驱动**——先写演讲稿（narration）→ TTS 合成音频（记录 audio_duration）→ 生成字幕（subtitle_cues 对齐时间轴）→ 合并输出。三字段驱动保证音画同步，避免早期"先视频后配音"的错位。

### Q36: 视频生成怎么避免画面空白？
**A**: v0.53 修复：prompt 要求每页 key_points（之前演讲稿 JSON 只返回 title+narration 导致帧要点区空白）；LLM 返回页数 > 大纲页数时不再越界（points 从 LLM 输出优先取）；视频页加关键帧静态图 + 每页说明防空白。

---

## 八、自我进化

### Q37: 四路自进化是哪四路？
**A**: 知识蒸馏（成功教学入库 evolved_*.json）/ 提示词补丁（evolve_prompt 提炼 subject_patches.md）/ 工具经验 / 新学科需求闭环（用户问未收录学科自动记录）。质量门禁（Constitutional AI 风格）过滤有害内容。

### Q38: SelfUpdateAgent 怎么工作？
**A**: 教学/对话后反思 → 生成结构化建议（self_update_suggestions.jsonl，7 分类）→ v0.25 起周期调度器落地执行（subject_addition 自动生成学科注册 JSON 入库 / library_update 记录 Library 待办）→ 建议回流 improvements.md → SelfEvolution.evolve_prompt 更新提示词。**越用越懂怎么教**。

### Q39: 自我进化会引入错误吗？怎么控制？
**A**: 质量门禁三重过滤：①有害内容过滤（Constitutional AI 风格）②平均分 <0.7 才触发反思（不是每次对话都改）③建议需落地执行器验证（生成 JSON 校验格式）。数据飞轮有刹车。

### Q40: 新学科需求闭环具体怎么落地？
**A**: 学生问"量子力学"（未收录）→ 记录需求 → SelfUpdateAgent 生成 subject_addition 建议 → 周期调度器生成 pending_subject JSON 到 Library/KnowledgeBase/subjects/ → 重启自动注册入库 → 下次可正常教学。从"用户需求"到"产品能力"的自动化闭环。

---

## 九、MCP 与工具

### Q41: MCP 双向打通是什么？
**A**: 对外暴露教育工具（mcp_gateway :8765 MCP Server，外部智能体可调用），对内调用外部标准工具（mcp_client MCP Client）——LLM 可用工具从 7 个扩到 34 个（filesystem 14 工具/memory 9 工具/pptx 生成）。MCP 连接 2/2 → 3/3。

### Q42: 插件体系（v0.53）和 MCP 什么关系？
**A**: 插件可被 opencode 识别调用（MCP 兼容）——PAEG 的能力边界可以扩展。10 个技能（concept-explainer/essay-feedback/knowledge-map/math-step-solver/study-planner/pdf/docx/xlsx/doc-coauthoring/teach）经 skill_registry → tool_registry 暴露为 LLM function calling。

### Q43: 文件 4 能力（用户上传文件）怎么实现？
**A**: lib/ingest/ 管线：readers 多格式提取（PDF/DOCX/MD/TXT）→ chunker 中文分块 → retriever BM25 + jieba（155 教育术语词典，无需向量数据库）→ intent_router 路由 → handlers 4 能力（找答案/基于文件讲解/输出原文/重组结构）。

### Q44: 知识导图功能怎么实现？
**A**: 说"画知识导图/列提纲/思维导图/知识结构"→ 触发知识导图技能（knowledge-map）→ 输出结构化知识地图（知识定位/知识树/关联/学习路径），用 KaTeX 渲染数学公式。

---

## 十、安全与合规

### Q45: SECRET_KEY 是怎么管理的？
**A**: v0.41 环境变量化（.env.example 模板，源码读 os.environ['SECRET_KEY']，缺失 fail-fast）；v0.51/0.52 **SECRET_KEY 双轨**——生产用环境变量，测试用固定值，避免互相污染。.gitignore 强化：users.json/users_data/uploads/data 不进版本控制。

### Q46: 依赖安全怎么保证？
**A**: pip-audit 加入 CI——已知 CVE 构建即失败（v0.51/0.52 修复 5 个高危依赖）。token 卫生：GitHub 推送脚本从 GH_TOKEN 环境变量读取，禁止硬编码。

### Q47: 提示词注入怎么防护？
**A**: 三层：①L0 保底安全规则（永不放开）②复合输入检测（v0.21.9 粘贴"帮我分析这段文字：<长文>"用 DeepSeek 结构化模板区分指令与资料）③质量门禁过滤有害内容。粘贴的文本作为"资料"而非"指令"处理。

### Q48: 用户隐私怎么保护？
**A**: 用户资料三级隔离——每个用户资料存在 Library/usr_knowledge/<uid>/，只检索自己的；私密数据（users.json/users_data/uploads）不入版本控制；AES 加解密（v0.52 修复乱码问题）；危机对话遵循危机协议（12356 热线）。

### Q49: 危机协议（自伤信号）怎么响应？
**A**: 检测到自伤/自杀信号 → LLM 先**完整回应用户说的话**（不机械短路）→ 自然融入关怀（12356 热线 + 继续聊天/现实陪伴的替代方法）→ 用户明确拒绝后尊重选择不再重复提示。危机钩子 _affection_gate_check 先行：危机时转 AffectionSupportor，不进入教学流水线。

---

## 十一、测试与质量保障

### Q50: 测试体系怎么组织的？
**A**: 分层测试：单元测试（pytest，tmp_path 隔离）/ 管线完整性契约（7 用例）/ API 契约（6 用例）/ 学段学科矩阵（5 用例）/ 全模式 E2E（chat/teach/mock 三模式）/ 多轮提示词注入实验（multi_turn_eval 5 维度：退化/决策/语言风格/约束/tool use）。核心功能回归 40+ 全过。

### Q51: audit_check 查什么？
**A**: 静态检视 24/24 全绿——P0：bare except: pass、写端点缺 _is_registered 校验、后端规范化；P1：展示质量（无英文残留）。每次改动前后必跑。

### Q52: 三处一致原则是什么？
**A**: 本地项目目录、GitHub 仓库（Golden2002/PAEG main）、Release 三者内容完全一致、互为备份。sync_check.py --fix 自动推送差异（本地为权威源）。2026-08-08：106 个文件全部一致，Release v0.34 最新。

### Q53: 为什么测试能并行执行？
**A**: v0.41 测试隔离根治——tests/conftest.py 统一 tmp_path fixture（pytest 内置），@pytest.fixture(autouse=True) 自动 patch data/ 路径，不再依赖 PAEG_TEST_MODE 环境变量。pytest -n auto 不再因共享文件冲突失败。

---

## 十二、工程化与可维护性

### Q54: server.py 从 4556 行拆到 ~4000 行是怎么做的？
**A**: 参考 Flask/Kraken/EAS Station/llama-index/langchain 六个成熟项目渐进拆分：Phase 1 config/+utils/（配置集中、纯函数）；Phase 2 infra/（12 运行时单例 + SESSIONS）+ services/（learner_session/polish/steering/routing/handlers 五模块）——server.py -550 行。拆分铁律：行为不变性（API 字节级一致）+ Expand-Migrate-Contract + ratchet。

### Q55: 模块化架构（v0.21）怎么做到"上架下架不改代码"？
**A**: paeg_modules.json 模块门控——14 个功能模块可独立启用/禁用（14 个功能模块）。运行时动态开关，禁用模块的端点/调度不生效。

### Q56: 结构决策怎么保证可追溯？
**A**: 每个重大结构选择都在《PAEG技术全景文档》§10.2.21 写明"为什么选 X 模式"（借鉴来源 + 应用方式）——未来回看避免重复决策，新协作者快速理解架构依据。维护手册沉淀架构地图/操作手册/孤儿清单。

---

## 十三、商业化与未来路线

### Q57: PAEG 的路线图是什么？
**A**: 当前：单用户完整闭环已验证（教学/倾诉/找答案/查资料/PPT/视频全功能实测 30/30 达标）。近期：多用户 SQLite（v0.38 已就绪）+ 语音（v0.36 edge-tts/Web Speech 已落地）+ 移动端/插件生态（v0.53 opencode 兼容）。远期：博雅教育垂直深化（35 学科 → 更多人文/哲学/批判性思维）+ 学校 B 端方案。

### Q58: 语音模块（v0.36）的边界是什么？
**A**: TTS（edge-tts 免 key 中文女声 zh-CN-XiaoxiaoNeural，后端 /api/voice/tts 生成 MP3）+ STT（浏览器 Web Speech API，Chrome/Edge/Safari）。STT 依赖 HTTPS 或 localhost 安全环境，微信内置浏览器（X5 内核）不支持——真实使用边界已在文档标注。

### Q59: 演示效果最好展示什么？
**A**: 三个杀手锏：①教学实测（孟德尔遗传 15 轮：从"眼睛颜色不一样"导入到独立挑战）②倾诉实测（学业压力 15 轮：从"我撑不住了"到"我会先从小目标做起"）③一键产出（对话 → PPT/视频文件）。30/30 轮全部达标，0 翻车。

---

## 十四、评委刁钻问题

### Q60: "这看起来就是个套壳 ChatGPT 的项目，技术含量在哪？"
**A**: 套壳 = 调 API 传 prompt。PAEG 做的是**教学过程的工程化**：9 个 subagent 的确定性调度、17 维画像的增量建模、8 层约束的动态控制、语言规范的程序化保证、学段学科的教学伦理约束、自我进化的数据飞轮——这些是 LLM 原生不提供的，需要完整的 Agent 架构设计。65 页附录展示了全功能实测证据，不是 PPT 演示。

### Q61: "LLM 幻觉怎么控制？"
**A**: 三层：①回答前强制检索（_safe_chat_with_retrieval 注入知识库结果，答案有据可依）②工具查证（web_search/verify_math 暴露给 LLM）③推荐类问题真联网不编造（网络检索徽章）。检索增强（RRF 融合）进一步降低幻觉——模型有资料可依，而非凭空生成。

### Q62: "评估分数可复现吗？"
**A**: 可以。评估用确定性启发式（非 LLM），同一回答永远得到同一分数——这是设计选择，为了教育场景的稳定反馈。测试用例验证了这一点。

### Q63: "如何验证教学效果？"
**A**: 双维评估（知识 + 过程）+ 双向证据（学生自述 + 实际表现）+ 30 场真实对话实测（30/30 达标，24 维结构化指标）。测试体系含管线完整性契约、API 契约、学段学科矩阵。教学效果通过"学生能独立推演"验证，而非学生说"懂了"。

### Q64: "一个团队怎么维护 40+ 模块？"
**A**: 模块化门控（paeg_modules.json）+ 分层架构（config/utils/infra/services/blueprints）+ 检视方法论（audit_check 24 项 + smoke_test 27 秒 + pytest 全量 + sync_check 三处一致 + arch_check 季度）+ 拆分铁律（行为不变性/可回滚）。**检视命令自动化，人只做增量**。

### Q65: "如果 DeepSeek 换掉或下线怎么办？"
**A**: LLM 层已抽象（LLM adapter + query() 统一接口，借鉴 LlamaIndex）——换模型只改配置。且 PAEG 的价值不在模型，在架构：教学闭环、画像、约束栈、语言规范都与具体模型解耦。换更强的模型反而更好（语言规范层保证输出质量）。

### Q66: "你怎么证明不是 demo 而是真能用？"
**A**: 65 页 PPT 附录即证据：教学/倾诉 15 轮**完整原文**（非截断）、找答案 10 题完整答案（含论述/计算/证明）、查资料 3 场景 sources 列表、30 场对话数据全景（24 维指标）。所有内容来自真实运行日志（v51_test_results.json 等），可独立复验。

---

*本文档基于 PAEG v0.53 真实代码/版本/实测数据编写。详细技术见《PAEG技术全景文档》。*
