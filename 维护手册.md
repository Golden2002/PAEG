# PAEG 维护手册（v0.36 ⭐ 基于两轮架构审查经验）

> 定位：与技术文档（PAEG技术全景文档.md）、元能力文档（元能力文档.md）并列的**第三份核心文档**——专门回答"哪里是什么、断了怎么办、怎么改不破坏"。
> 审查经验来源：v0.36 两轮全面审计（死代码 78 发现 + 提示词 19 项 + subagent 5 项 + 前后端对齐）+ 连接图（ARCHITECTURE_LINKS.md）。
> 核心理念：代码像高楼大厦，每个模块是砖块瓦块——**整洁、可查、可替换**。

---

## 一、架构地图（连接图速查）

> 完整连接图见 ARCHITECTURE_LINKS.md（L0 总览 + L1 细分 mermaid）。此处是维护视角的浓缩版。

### 1.1 请求链路（前端 → 后端 → subagent → LLM）

```
前端 index.html (3351行, 22个API调用, 12种SSE消费)
  → Flask server.py (4221行, 47路由)
    → 路由层：@require_module 门禁 (12模块) + LLM意图路由 (route_intent 14意图)
    → PAEG主类 (paeg.py, 持有9个subagent)
      → 教学管线：危机门禁 → Diagnostor → Planner → Presenter → Evaluator → Adapter
      → 辅助：AnswerSolver(找答案) / AffectionSupportor(情绪) / ResourceLibrarian(资料) / SelfUpdateAgent(建议) / Individuality(画像)
    → 工具层：7内置工具 + 10 Skills + 3 MCP (filesystem/memory/pptx) → 24工具
    → 自进化：SelfUpdater / SelfEvolver / SelfEvolution / SelfImprover / PeriodicSelfUpdater
  → LLM (DeepSeek)
```

### 1.2 关键文件职责（维护者必读）

| 文件 | 行数 | 职责 | 维护注意 |
|---|---|---|---|
| server.py | 4221 | Flask 路由 + 早退分支 + SSE | **最大文件**；改早退分支必测完整管线 |
| subagents.py | 2382 | 9 个 subagent | Presenter 是教学核心；Individuality 做画像 |
| prompts.py | 1294 | 全部提示词（学科风格/语言规范/意图 prompt） | **改提示词影响所有教学输出** |
| meta_router.py | 862 | LLM 意图路由 + 规则兜底 | 14 意图选项与规则函数同名 |
| paeg.py | 714 | PAEG 主类（教学主循环） | 9 subagent 实例化于此 |
| tool_registry.py | 450 | 工具注册 + run_agent_loop | 7 内置工具 + Skills + MCP 合并 |
| module_registry.py | 120 | 12 模块门禁 | 3 模块（mcp/weather/knowledge_map）无独立路由 |

---

## 二、审查经验教训（两轮审计总结）

### 2.1 教训 1：审计必须以主项目为准

- **错误**：基于 wbo 下 paeg_work（废弃旧版）审计 → 4 次误报（subject-tree/avatar/pptx/PeriodicUpdater 都"缺失"）
- **正确**：主项目 = `D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\`；wbo 下 paeg_link/paeg_project 是 junction（内容相同），**paeg_work 是 v0.22.2 废弃版**
- **规则**：任何"缺失/未接线"判断，先在主项目 grep 确认（可能有 junction 副本混淆）

### 2.2 教训 2：提示词层有真实矛盾（P0 级）

两轮审计发现提示词自相矛盾（LLM 会困惑）：

| 矛盾 | 位置 | 修复建议 |
|---|---|---|
| "单字'看/写'禁用" vs "推荐'看''写'" | prompts.py L109 vs L153 | 改 L109 为"抽象单字'用/学/搞/弄'禁用" |
| "句子要短" vs "句子完整含状语" | L29 vs L127 | 加注"短≠残缺" |
| "祈使可省主语" vs "'别贪多'→'你不要贪多'" | L83 vs L104 | 加注边界 |
| "人话优先" 可能架空 v0.35 总纲 | L957 | 加注"v0.35 完整句是底线" |

**规则**：改提示词时检查是否与 LANGUAGE_STYLE 其他章节冲突。

### 2.3 教训 3：subagent 大部分健康，个别闲置

- **主线**（PAEG 持有 + 真调用）：Diagnostor/Planner/Presenter/Evaluator/Adapter/AffectionSupportor/Individuality
- **server 直调**：AnswerSolver/ResourceLibrarian/SelfUpdateAgent
- **闲置**：SelfUpdateAgent 在 paeg.teach 主循环只初始化未调用（靠 /api/self-update/from-feedback 触发）；SelfImprover 实例化零调用

### 2.4 教训 4：前后端对齐有真实体验缺口

- **前端 22 个 API 调用 100% 命中后端**（零 404）——基础联通完整
- **最大体验缺口**：teach_stream 发 12 种 SSE 事件，前端原来只消费 7 种 → 教学 6 步推理（诊断/规划/讲解/评估/调整/反思）不可见（**v0.36 已修复**，现消费全部 12 种）
- **孤儿路由 11 个**：threads 系列/solve/chat 同步/batch 等前端未调用

---

## 三、维护操作手册（按场景）

### 3.1 改一个学科的教学提示词

```bash
# 1. 改 prompts.py 的 SUBJECT_STYLES[学科]（persona/language/structure/emphasis/subfield_guide）
# 2. 语法检查
python -c "import ast; ast.parse(open(r'D:\...\prompts.py', encoding='utf-8').read())"
# 3. 回归（presenter 测试 + 综合测试）
python -m pytest tests/test_presenter.py tests/test_v033_comprehensive.py -q
# 4. 浏览器实测：问该学科问题，看输出风格
```

**注意**：学科 subfield_guide 有 23 个是 2 行简略版（仅 physics/math/linguistics/atmospheric_science/CS/AI 详细）——增强时参照详细版格式。

### 3.2 新增/删除一个 API 路由

```bash
# 新增：server.py 加 @app.route + @require_module("模块名")
# 删除：先确认无前端调用（grep index.html），再删；孤儿路由列表见 §四
# 验证：
python -m pytest tests/test_v028_endpoints.py -q  # 端点测试
```

### 3.3 排查"教学不工作/答非所问"

```bash
# 1. 看意图路由判断（LLM 优先）
python -c "import sys; sys.path.insert(0, r'...'); from meta_router import route_intent; print(route_intent('法语学习的软件有什么推荐', llm=None))"
# 2. 看是否走完整管线（须含 diagnosis 事件）
# 3. 检查 meta-log（是否写入 user_modeling）
# 4. 浏览器 Network → teach_stream → EventStream 看事件序列
```

**常见坑**：
- 推荐类问题被误判知识库 → v0.35 已修（is_recommend_request + LLM 路由）
- 教学走早退分支 → 无 diagnosis 事件（检查意图路由）
- 元认知日志显示对话历史 → Individuality 建模输入不足（v0.35.1 已修：画像入 prompt）

### 3.4 重启服务

```powershell
# 杀 5000 端口 → 重启
Get-NetTCPConnection -LocalPort 5000 | ForEach-Object { taskkill /F /PID $_.OwningProcess }
cd D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\05_实现原型
python server.py
```

### 3.5 三处一致（本地 ↔ GitHub ↔ Release）

```powershell
$env:GH_TOKEN='<token>'
python D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\sync_check.py --fix
```
**规则**：改完代码/文档/测试 → 跑 sync_check --fix → 校验报告 0 差异。

---

## 四、已知孤儿/死代码清单（维护者决策）

### 4.1 建议清理（前端零调用）

| 路由/模块 | 位置 | 建议 |
|---|---|---|
| /api/threads/* (4个) | server.py:246-316 | 删除（conversations 替代） |
| /api/solve | server.py:3336 | 删除（answer 替代） |
| /api/chat (同步) | server.py:2979 | 删除（chat/stream 替代） |
| /api/batch | server.py:1884 | 删除（self-update/run 替代） |
| assets/vendor/mathjax/ | 前端目录 | 删除（KaTeX 替代） |

### 4.2 暂保留（管理/调试用）

| 路由 | 用途 |
|---|---|
| /api/knowledge/<id> | 知识图谱详情 |
| /api/skills | 技能清单 |
| /api/self-update/* (3个) | 自进化管理 |
| /api/modules | 模块开关状态 |

### 4.3 已清理（v0.36 完成）

| 项 | 说明 |
|---|---|
| /api/resources 死代码 (70行) | 重复定义，删除简化版，保留 resource_lookup 完整版 |
| 教学路径 skill 注入 | Presenter.run 单点注入，10 技能教学可用 |
| 教学 6 步前端可视化 | 前端消费 diagnosis/plan/step/evaluation/adjustment/reflection |
| avatar ok:false bug | 错误响应补字段，前端不误判 |

---

## 五、核心概念速查（维护必懂）

### 5.1 LLM 优先意图路由（v0.35）

```
route_intent(text, llm) → {intent: 14选项之一, confidence, reason}
  → confidence ≥ 0.6：用 LLM 分类
  → 否则：rule_fallback_intent（规则兜底）
  → 教学请求（teach/answer）：强制走完整管线（建模+meta-log）
  → 危机/自伤：规则 fast-path（安全 > 延迟）
```

### 5.2 教学完整管线（12 事件序列）

```
diagnosis → retrieval → plan → step → presentation → evaluation → adjustment
→ reflection → self_update → summary → self_evolution → doc → done
```
**判定标准**：教学请求必须含 diagnosis 事件（否则走了早退分支）。

### 5.3 语言规范总纲（v0.35，L1 提示词源头治理）

7 项自查口诀：有主语吗？有谓语吗？有宾语吗？介词用了吗？词形完整吗？修饰够吗？状语有吗？
规则层（language_refiner.py）只兜底，主责在提示词。

### 5.4 自进化四路（+周期调度）

| 模块 | 职责 | 触发 | 产物 |
|---|---|---|---|
| SelfUpdater | 数据落盘 | paeg.teach 后 | reflections.json |
| SelfEvolver | 微反思+周度洞察 | on_session_end + 周期 | insights.json |
| SelfEvolution | 4 路落盘（KB/提示词/工具/洞察） | evolve_prompt + distill | subject_patches.md |
| SelfImprover | 失败案例学习 | 周期 | cases.jsonl/improvements.md |
| PeriodicSelfUpdater | 后台调度 | server 启动 start() | 各模块入口 |

---

## 六、测试体系速查

| 测试层 | 文件 | 命令 |
|---|---|---|
| 单元/API | tests/test_v028_endpoints.py | pytest -q |
| 学段矩阵 | tests/test_v032_grade_subject_matrix.py | pytest -q |
| 综合 | tests/test_v033_comprehensive.py | pytest -q |
| 契约 | tests/test_contracts.py | pytest -q |
| 管线完整性 | tests/test_pipeline_integrity.py | pytest -q |
| SSE 捕获工具 | tests/sse_helpers.py | import 用 |

**回归门槛**：改核心链路（server.py/subagents.py/prompts.py）后，至少跑 test_v033 + test_contracts + test_pipeline_integrity。

---

## 七、维护 checklist（每次改动前）

- [ ] 主项目路径确认（非 paeg_work）
- [ ] 改动文件语法检查（ast.parse）
- [ ] 提示词改动查冲突（LANGUAGE_STYLE 一致性）
- [ ] 路由改动查前端调用（grep index.html）
- [ ] SSE 事件改动查前端消费
- [ ] 核心链路回归（test_v033 + contracts + pipeline）
- [ ] 三处一致（sync_check --fix）
- [ ] CHANGELOG + 技术文档 + 元能力文档同步

---

*维护手册 v0.36 · 2026-08-09 · 基于两轮架构审查 + 连接图经验*
---

## 八、项目文件夹整理（v0.36 防误改）

> **教训**：曾因 wbo 下存在废弃旧版 paeg_work，导致 agent 误改旧版而非主项目（P0 修复改错副本）。

### 当前目录结构（整理后）

| 位置 | 目录 | 状态 |
|---|---|---|
| **D 盘主项目** | `D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\` | ✅ **唯一权威** |
| D 盘归档 | `归档_废弃副本\paeg_work_v0222_废弃` | 旧版副本（保留历史） |
| D 盘归档 | `归档_废弃副本\paeg_audit_v05_孤立` | 孤立旧文件（保留历史） |
| wbo | `paeg_link` / `paeg_project` | junction 指向主项目（合法链接） |

### 铁律
1. **所有修改只在 D 盘主项目**——wbo 下只有链接，无物理副本
2. **废弃旧版已归档**（不在 wbo）——不会再被误改
3. **agent 任务必须确认路径**：改前 `Test-Path` 主项目，拒绝 paeg_work 类路径
4. 审计"缺失"前先在主项目 grep（避免链接/副本混淆误报）

---

## 四、标准化检视方法论（v0.39 ⭐ 新）

> 基于业界方法论（测试金字塔/代码审查/架构审查/安全/数据/LLM 特有/CI 门禁/Python 专项）
> + 项目结构洞察（早退分支/持久化/接线/测试盲区）整合。**核心：把检视标准变成可执行脚本**。

### 4.1 一键检视命令

```bash
cd D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\05_实现原型
python audit_check.py          # 标准化检视（7 维度 15 项）
python smoke_test.py           # 快速冒烟（27 秒，不卡住）
python -m pytest tests/ -q     # 全量回归
python sync_check.py           # 三处一致（本地/GitHub/Release）
```

### 4.2 7 大检视维度（audit_check.py 实现）

| 维度 | 检查项 | 等级 | 教训来源 |
|---|---|---|---|
| 早退分支 | 所有 gen_ 生成器必须保存历史 | P0 | v0.36.3 "15 个早退分支跳过历史保存" |
| 静默异常 | 无 except:pass 静默吞异常 | P0 | v0.37.1 "12+ 处静默吞异常" |
| 接线完整 | 前端 API 全部有后端路由 | P0 | 前后端契约对齐 |
| 持久化 | 写安全（锁+内存上限+SQLite） | P0 | v0.37.2 WinError 32 + v0.38 SQLite |
| 版本一致 | 核心文件版本号对齐 | P1 | v0.38.1 "7 文件版本号过时" |
| 测试盲区 | 高流量端点有测试 | P1 | v0.34 测试方法论反思 |
| 数据健康 | users_data 精简 + 无大快照 | P1 | v0.38.1 清理 |

### 4.3 检视流程（每次发布前必做）

1. **`python audit_check.py`**：P0/P1 必须全过，P2 记录
2. **`python smoke_test.py`**：端点在 27 秒内验证（不调完整 LLM）
3. **`python -m pytest tests/ -q`**：全量回归，新增用例必须过
4. **`python sync_check.py --fix`**：三处一致推送
5. **生成 Release 快照**：snapshots/snapshot_vX.Y_时间戳.zip

### 4.4 检视铁律（防"修复未生效"复发）

1. **任何早退分支必须 _save_teach_turn**（回归测试会抓）
2. **禁止 bare except: pass**——必须打印日志（audit_check 抓）
3. **每个写端点必须 _is_registered 校验**
4. **前端读字段必须双兼容**（mastery/level 教训）
5. **大测试后台运行**，不阻塞对话（smoke_test 27 秒原则）

### 4.5 ⭐ 反思教训：为什么有些功能"改了很多次还是改不对"（v0.40.5）

> 用户反馈元认知用户名、深色主题等反复修复仍不对，根源不是单个 bug，而是**验证方法缺陷**。四大根因：

1. **"代码存在"≠"行为正确"**：多次以 grep 到修复标记作为完成证据，没在真实浏览器/真实数据上验证。深色主题代码一直在（6 快照一致），但用户体验是"失效"（localStorage 固化）；元认知用户名代码有 displayName，但 STATE.nickname 默认"学习者"且未读 localStorage 真实昵称。
   - **铁律**：完成标准 = "用户看到的真实行为正确"，不是"代码里有"。

2. **数据流理解不完整就动手**：元认知用户名 bug 根因是 logs 无 nickname 字段 + STATE 未初始化——没先查清"显示的数据从哪来"就改前端，必然改不对。
   - **铁律**：先查数据来源（后端返回什么/前端何时初始化），再改代码。

3. **追加补丁代替整体重写**：元认知日志改 3 次（建模→去 emoji→加用户名）都是叠补丁，没验证其他部分是否被破坏。
   - **铁律**：功能复杂时重写+整体验证，不叠补丁。

4. **修复没同步验证依赖方**：改 chat_stream 空输入时 oldString 匹配两处（chat_stream+general_chat），没先确认函数边界。
   - **铁律**：改前确认唯一锚点（函数 docstring 边界）。

### 4.6 ⭐ 更新后验证方法论（v0.40.5）

**新增"行为验证"层**（在 audit_check 静态检视之外）：
1. **真实数据验证**：每个前端显示功能，用真实登录账号（如 u3"团聚体"）跑一遍，确认看到正确值
2. **localStorage 场景验证**：测试"首次访问（无 localStorage）"和"已登录（有 localStorage）"两种路径
3. **浏览器强制刷新**：修复后必须清缓存验证（旧 index.html 缓存会掩盖新代码）
4. **综合测试**：`python tests/high_intensity_http_test.py`（29/33 通过基线，4 项待 UTF-8 复测）
5. **三处一致**：本地 / GitHub / Release 完整包（236MB，含全部源码+数据+Library）

## 六、成熟项目结构借鉴（v0.41）⭐

> **背景**：server.py 已达 ~4500 行，单文件维护窗口触顶。v0.41 启动"渐进式拆分"——参考 Flask / cookiecutter-flask / Kraken / EAS Station / llama-index / langchain 六个成熟项目的结构做迁移。本节是迁移的**操作手册**。

### 6.1 目标分层（从 4500 行单文件到分层架构）

```
05_实现原型/
├── server.py              # 入口薄壳（仅 app factory + 蓝图注册，目标 < 500 行）
├── config/                # 配置层（settings.py / secrets.py / env loader）
├── utils/                 # 纯函数工具（text/json/time — 无副作用）
├── services/              # 业务服务（tts_service / user_service / llm_service）
├── blueprints/            # HTTP 蓝图（api_bp / admin_bp / voice_bp）
├── agents/                # subagent 实现（planner / presenter / evaluator）
├── infra/                 # 基础设施（db / cache / file_lock / audit）
└── tests/                 # 镜像结构测试
```

### 6.2 拆分顺序（低风险先行 · 每步可回滚）

| 顺序 | 拆分层 | 风险 | 依赖 | 预期收益 |
|---|---|---|---|---|
| **Step 1** | `config/` | 低 | 无 | 集中管理 secrets/env，便于测试注入 |
| **Step 2** | `utils/` | 低 | 无 | 纯函数零副作用，回归容易 |
| **Step 3** | `services/` | 中 | utils | LLM 调用独立可 mock，测试提速 |
| **Step 4** | `blueprints/` | 中 | services | HTTP 路由独立维护，前端契约清晰 |
| **Step 5** | `agents/` | 高 | services | subagent 独立单元化，AI 行为可观测 |
| **Step 6** | `infra/` | 低 | services | 文件锁/审计独立，单测不需要真实文件 |

**铁律**：每步只拆一层，跑全量回归通过后再走下一步。

### 6.3 拆分操作三件套（Expand-Migrate-Contract）

每一步执行严格的三阶段：

1. **Expand（扩展）**：在新模块创建同名函数/类，**同时**保留旧实现
   - 验证：新旧两路径同时可用，行为完全一致
   - 测试：旧路径回归通过 + 新路径 smoke test 通过
2. **Migrate（迁移）**：把所有调用点从旧实现切到新模块
   - 验证：grep 旧实现引用 → 0 匹配
   - 测试：全量回归通过
3. **Contract（收缩）**：删除旧实现代码
   - 验证：git diff 确认删除的是死代码（无引用）
   - 测试：全量回归 + smoke_test.py + pytest

**每一步必须可回滚**：保留 git tag 或分支，回退命令 `git revert` 或 `git reset --hard <tag>`。

### 6.4 每个拆分的验收标准

每个 Step 完成必须满足以下**全部**条件：

- [ ] **静态检查**：`python audit_check.py` 0 P0 / 0 P1
- [ ] **端点验证**：`python smoke_test.py` 27 秒内全绿
- [ ] **全量回归**：`python -m pytest tests/ -q` 全部通过（无新增 skip/xfail）
- [ ] **行为不变性**：拆分前后对同一请求的响应字节级一致（用归档快照对比）
- [ ] **行数下降**：server.py 行数净减少（不允许"复制+导入"假拆分）
- [ ] **导入卫生**：新模块无循环引用（`python -c "import server"` 启动 0 警告）
- [ ] **文档同步**：更新技术全景文档 §10.2.21 + CHANGELOG 记录拆分步骤

### 6.5 已落地：Phase 1（v0.41 完成）

- **Step 1 完成**：`config/` 拆分（settings.py + secrets.py + env loader）
- **Step 2 完成**：`utils/` 拆分（text_utils / json_utils / time_utils 纯函数集）
- **行为不变性**：拆前拆后 smoke_test.py 27 秒内全绿，API 响应字节一致
- **回归**：pytest 全部通过，0 新增 skip

### 6.6 进行中：Phase 2（v0.41+）

- **Step 3 进行中**：`services/` 抽离 LLM 调用（tts_service / llm_service）
- **风险点**：LLM 调用涉及 subagent 调度，需先做 services/agents 边界梳理
- **下一步**：Step 4 blueprints/ 拆分（依赖 services 稳定）

### 6.7 拆分过程的反模式（禁止）

- **复制+导入假拆分**：把代码从 A 复制到 B，A 改 `from B import`——表面分层，实际双份维护。**禁止**
- **大爆炸一次性拆完**：没有中间态回滚点——一次失败回退成本极高。**禁止**
- **拆分同时改逻辑**：借拆分之名重构 API 行为——回归安全网失效。**禁止**
- **跨步并行**：Step 3 没跑通就开 Step 4——失败难定位。**禁止**

### 6.8 参考项目结构要点速查

| 项目 | 看什么 | URL |
|---|---|---|
| Flask 官方 | Application Factory 模式 | flask.palletsprojects.com/patterns/appfactories |
| cookiecutter-flask | 项目骨架标准布局 | github.com/cookiecutter/cookiecutter-flask |
| Kraken | 大型 Flask 单体拆分 | github.com/kraken-tech/kraken |
| EAS Station | 工具注册表 + 模块门控 | github.com/ggelashvili/EAS-Station |
| llama-index | `query()` 包装 + 多 agent | github.com/run-llama/llama_index |
| langchain | Tool Registry 集中管理 | github.com/langchain-ai/langchain |

