# PAEG 元能力问答文档（路演答辩准备）

> 版本：v0.53 | 更新：2026-08-11 | 用途：路演/答辩 Q&A 速查
> 元能力 = 生成策略/工作流/设计决策，区别于纯技术实现

---

## 一、布局系统（PPT 错位根因的元层面）

### Q1: 为什么 PPT 元素"摆不正"？根本原因是什么？
**A**（元层面）: 不是单一元素错，是**布局系统缺失**——
- 生成时把装饰（圆点/编号/图标）与内容（文本/对话）**混在同一坐标系手写定位**
- 没有"**内容流 + 装饰层**"的分离架构：内容应自动排布，装饰应**相对锚定**内容区边缘
- 每页单独手写绝对坐标 → 换内容就重叠 → 必须**重新设计布局系统**

### Q2: 布局系统应该怎么设计才不重叠？
**A**: 三原则：
1. **分层**：内容层（自动流式排布）与装饰层（绝对定位但避开内容区）分离
2. **锚定**：装饰锚定内容区边缘/角落，不手写全页绝对坐标
3. **校验**：生成后必跑碰撞检测（bounding box 相交扫描），重叠 >30% 面积自动重排

### Q3: 为什么早期没发现？后续如何避免重蹈覆辙？
**A**: 早期 QA 只查"内容正确性"（文本/数据），**没查布局重叠**。教训：**QA 循环必须含几何校验**（逐页扫描重叠/溢出页边距），并纳入生成 prompt 的形状数量上限（≤20/页）。

---
