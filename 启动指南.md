# PAEG 启动指南（更新电脑后恢复服务）

> 适用：电脑重启/更新后，恢复 ①PAEG 后端服务 ②wbo 微信远程指挥 ③公网访问
> 版本：v0.41.9 · 2026-08-10
> 参考：README.md / 维护手册 §3.4（重启服务）/ intermediate/02_v08_公网部署

---

## 一、总览（启动顺序）

```
1. 启动 PAEG 后端（python server.py）—— 核心，其他都依赖它
2. 验证本地访问（http://127.0.0.1:5000）
3. 启动 wbo（微信远程指挥）—— 需要手机端继续用
4. （可选）启动公网隧道（Cloudflare）—— 手机/外部访问
```

---

## 二、启动 PAEG 后端

```powershell
# 1. 确认主项目路径（唯一权威，别改副本）
Set-Location "D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\05_实现原型"

# 2. 检查是否已有服务在跑（有则先杀，防端口占用）
Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue | 
    ForEach-Object { taskkill /F /PID $_.OwningProcess }

# 3. 启动（后台，隐藏窗口）
Start-Process -FilePath "python" -ArgumentList "server.py" -WindowStyle Hidden

# 4. 等 15-20 秒（LLM/知识库初始化），验证
Invoke-WebRequest -Uri "http://127.0.0.1:5000/api/health" -UseBasicParsing -TimeoutSec 10
# 期望：200
```

**验证**：
- 浏览器打开 `http://127.0.0.1:5000` → 应看到教学界面
- 测试问一句"什么是质数" → 应正常教学

**故障排查**：
| 症状 | 原因 | 解决 |
|---|---|---|
| health 超时 | 初始化慢/端口占用 | 等更久；查 `Get-NetTCPConnection -LocalPort 5000` |
| 教学无内容 | 服务异常 | 看启动日志（如加了 -RedirectStandardError 到文件） |
| SECRET_KEY 警告 | 生产环境需设置 | `$env:PAEG_SECRET_KEY="xxx"` 后重启（开发可忽略） |

---

## 三、启动 wbo（微信远程指挥）

```powershell
# 1. 启动（前台，会留一个 PowerShell 窗口，别关）
cd D:\wbo-workspace
wbo

# 或后台：
# wbo --daemon
```

**验证**：
- 微信里给 ClawBot 发 `/status` → 应回"Running"
- 发普通文字 → opencode agent 应回复

**故障排查**（README 详述）：
| 症状 | 解决 |
|---|---|
| ByteString 错误 | 工作目录含中文 → 用 ASCII 路径（D:\wbo-workspace 即可） |
| 微信 10 条消息限制 | 发 `/next` 重置计数 |
| 端口 4096/18792 占用 | `Get-NetTCPConnection -LocalPort 4096,18792 \| taskkill` 后重启 |
| token 失效 | `wbo stop` + `Remove-Item $env:USERPROFILE\.wechat-bridge-opencode\auth\token.json` + `wbo --login` 重扫 |

---

## 四、启动公网服务（Cloudflare Tunnel）

```powershell
# 方案 A：临时隧道（每次启动 URL 会变）
& "D:\devtools\cloudflared.exe" tunnel --url http://127.0.0.1:5000
# 输出里有 https://xxx.trycloudflare.com —— 手机/外部用这个地址

# 方案 B：正式固定域名（需已配置 named tunnel）
& "D:\devtools\cloudflared.exe" tunnel run <tunnel-name>
```

**注意**：
- 公网访问时**语音功能受限**（麦克风需 HTTPS，trycloudflare 自带 HTTPS ✓ 但需浏览器授权）
- 公网 URL 用于演示/外部访问；日常开发用 `http://127.0.0.1:5000`

---

## 五、一键脚本（推荐保存）

保存为 `D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\启动服务.ps1`：

```powershell
# PAEG 一键启动：后端 + 健康检查
Set-Location "D:\桌面\智能体架构与开发（含大模型）\14_教育者Agent项目\05_实现原型"
Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue |
    ForEach-Object { taskkill /F /PID $_.OwningProcess }
Start-Sleep -Seconds 2
Start-Process -FilePath "python" -ArgumentList "server.py" -WindowStyle Hidden
Write-Host "PAEG 后端启动中，等 18 秒..."
Start-Sleep -Seconds 18
try {
    $r = Invoke-WebRequest -Uri "http://127.0.0.1:5000/api/health" -UseBasicParsing -TimeoutSec 10
    Write-Host "PAEG 就绪: HTTP $($r.StatusCode)"
} catch {
    Write-Host "PAEG 启动失败，检查日志"
}
```

保存为 `D:\wbo-workspace\启动wbo.ps1`：

```powershell
# wbo 启动
Set-Location D:\wbo-workspace
wbo
```

---

## 六、常见场景速查

| 场景 | 命令 |
|---|---|
| 只启动 PAEG | 运行 启动服务.ps1 |
| 启动 PAEG + wbo | 先 启动服务.ps1，再 启动wbo.ps1 |
| 全部（含公网） | 上面两个 + cloudflared 方案 A |
| 重启 PAEG | 运行 启动服务.ps1（自带杀旧进程） |
| 重启 wbo | wbo stop; 等 3 秒; wbo --daemon |

---

*指南版本 v0.41.9 · 由 Sisyphus 编写 · 如有更新请同步 CHANGELOG*
