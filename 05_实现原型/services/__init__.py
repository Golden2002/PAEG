"""services package for PAEG server.

Sub-modules split out of server.py to reduce code debt (v0.42+).
This package currently hosts pure refactors; behaviour must remain
identical to the original inlined snippets — see unit-by-unit history
in git for the original sites that each helper replaces.
"""
