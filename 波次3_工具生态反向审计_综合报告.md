# 波次 3 工具生态反向审计 · 综合报告

> 时间：2026-08-28 · 类型：基线能力反向审计综合（升级版 vs 基线）+ 熔断判定 + 补缺 backlog
> 依据：总需求与执行标准 §4.6.1（能力不退化铁则 T1 / 基线锚定铁则 T2 / 反向审计铁则 T3 / 熔断机制③）

## 一、四工具审计结果汇总

| 工具 | 仓库 | 基线 | 保留 | 缺失 | 退化 | 测试 | 熔断 |
|---|---|---|---|---|---|---|---|
| 语言规范校对 | 14.1_paeg-lang-style-plugin | L-01~L-12 | 9 | **1 (L-08)** | **2 (L-09/L-10)** | 83 绿 | 🔴 **熔断** |
| 外语词汇表 | 14.3_paeg-vocabulary-plugin | 50 项 | 50 | 0 | 0 | 154 绿 | ✅ 放行 |
| 法律检索 | 14.4_legal-research-skill | 115 项（A-K） | 115 | 0 | 0 | 38+1skip | ✅ 放行 |
| 通用简历 | 14.5 主 + 14.6 辅 | 主 72 + 辅 82 | 154 | 0 | 0 | 109/300/262 | ✅ 放行 |

**核心结论**：词汇表、法律、简历三工具代码已 100% 保留基线能力（法律经 `git diff 5a1e747` 证明字节级零变更）；**仅语言规范校对触发熔断**——缺失 1 项、退化 2 项、测试缺口 2 项。

## 二、熔断判定（能力退化熔断机制）

- **语言规范校对**：熔断触发（缺失 L-08 + 退化 L-09/L-10），违反能力不退化铁则，**不放行**，须补缺后重新审计。
- **其余三工具**：三条件均未触发，放行。

## 二·补 · 补缺与接线修复结果（2026-08-28）

> 熔断项已按 §三 backlog 逐项 TDD 修复，接线断点已闭合。

**补缺（语言规范校对 5 项全闭合）**：L-08 `polish_text` 已实现 / L-09 语料路径已修 / L-10 禁词三键已合并 / L-05·L-11 测试已补 → 测试 83→**90 passed**，熔断解除，**放行**。

**接线修复（Oracle 4 断点全闭合）**：
1. ✅ legal-research `mcp_servers.json` 陈旧 `D:/wbo-workspace` 路径 → 真实 `14.4` 路径
2. ✅ `paeg-lang-style` 注册进 `mcp_servers.json`（`paeg-lang-style-mcp` console script）
3. ✅ resume MCP 4→**21 工具**（`mcp_server.py` 补 17 个 `@mcp.tool` 注册，全量暴露 schema 20 工具 + list_tools）
4. （`mcp_client` 仅 `list_tools` 属三原语增强项，4 工具当前均未暴露 resources/prompts，列为顶尖标准后续）

**端到端验证**：主项目 server 启动 → `/api/health` `mcp 6/6 connected, 70 tools` → `/api/mcp/tools` 70 工具动态发现 → 前端 `index.html`（267KB）含气象/词汇表/简历/备课/实用工具入口。**前端↔后端↔主项目↔工具接线无断点**。

## 三、补缺 backlog（仅语言规范校对 · 逐项 TDD）

| # | 项 | 类型 | 现状 | TDD（红→绿） |
|---|---|---|---|---|
| 1 | **L-08** `polish_text` 全局守门兼容入口 | 🔴 缺失 | `services/polish.py` 未迁移，仅存 `gate.py` 的 `polish_fn=None` 注入钩子 | 红：`test_polish_text_triggers_refine`（含"听着你"文本触发 refine 且去除）+ `test_polish_text_silent_fallback`（refine 抛异常→回退原文）；绿：实现 `polish_text`（AI味/省略句/动宾→条件 refine→try/except 回退）|
| 2 | **L-09** 薇依语料 few-shot | 🟡 退化 | 默认路径指向不存在的 `src/paeg_lang_style/data/`，语料实际在仓库根 `data/`，默认 `corpus=[]` | 红：`test_corpus_loaded_by_default`（`len(corpus)>=6`）；绿：修默认路径或移语料入包 |
| 3 | **L-10** 外部禁词可配置 | 🟡 退化 | 默认路径失效（`load_json()→added=0`）+ `forbidden.py:160` 只读两键跳过 `pseudo_empathy_verbs`（三类→两类） | 红：`test_load_json_merges_three_categories`（`detect("被听见")`命中）+ `test_load_json_default_path`（`>0`）；绿：读三键 + 修默认路径 |
| 4 | **L-05** 高频词重复检测 | ⚪ 测试缺口 | 实现完整（`refiner.py:221-257`），无测试断言 | 红→绿：`test_word_repetition`（"重量"×3 触发；窗口外不触发）|
| 5 | **L-11** 静默降级安全 | ⚪ 测试缺口 | try/except 完整，无异常注入测试 | 红→绿：`test_silent_fallback_on_exception`（refine 抛 RuntimeError→返回原文）|

## 四、外部调研要点（4 librarian · 供补缺与接线参考）

- **语言规范校对**：TextGuard `PROOFREAD_TYPES`（typo/grammar/punctuation/style/sensitive/logic）+ `{original,suggestion,explanation,severity}` 修订痕迹 schema；gongwen-lint `Finding{rule,message,suggestion}` 规则引擎；simple-csc faithfulness reward 保原意；zhlint 标点规则。
- **法律检索**：北大法宝官方 MCP（mcp.pkulaw.com，`citation_validator`/`law_recognition` 服务）；Legal-Skills-Chinese `legal-norm-validity-check` 7 态效力状态机；CaseMatch-Agent BGE-M3+BM25 类案混合检索 + 可解释理由；LeCaRD 评测基准。
- **简历**：srbhr/Resume-Matcher `keyword-matcher.ts`；rendercv `scripts/ats_proof/analyze_pdfs.py`（PDF 文本层/阅读顺序校验）+ `test_pdf_png.py`（编译视觉对比）；HackMyResume 多格式导出；careerproof-ai STAR bullet prompts。
- **词汇表**：GLM-OCR `zipf_frequency>=2.5` 残词修复；VocabSieve 句挖矿（`sentence.splitter` + 加粗目标词）；wiktextract/kaikki IPA+词源+多义项；vsnt.py 词表 CSS 模板。

## 五、后续波次（按依赖顺序）

1. **补缺**：语言规范校对 5 项 TDD 修复（红→绿留证据）→ 重跑 pytest → 更新 §四熔断状态。
2. **接线审计（Oracle 论证）**：前端↔后端↔主项目↔工具内部，验证 MCP 动态发现（tools/resources/prompts list）+ 无断点。
3. **主项目 Playwright 端到端**：安装全工具生态后逐功能测试。
4. **vision subagent 审 CSS 模板**（Bell Jar / 生命现象）。
