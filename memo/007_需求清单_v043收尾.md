# 需求清单 · v0.43 收尾（2026-08-10）

> 来源：今日对话历史需求收集 + bug 修复 + Oracle 全项目检视 + 同类错误探索
> 状态：收集汇总中（T2）→ 实施（T5）

---

## 一、今日已提出并完成的需求（✅）

| # | 需求 | 状态 | 提交 |
|---|---|---|---|
| 1 | 注册问卷（10 题选择题：学段/风格/动机/深度/节奏/时段/老师性格/薄弱擅长/目标） | ✅ 完成 | c429410 |
| 2 | 问卷 → 进入用户建模（Individuality 优先采用） | ✅ 完成 | c429410 |
| 3 | 问卷 → 固定提示词注入所有 6 模式（每次 LLM 调用都带） | ✅ 完成 | c429410 |
| 4 | 学习深度维度（depth_pref：basic/intermediate/advanced → LLM 深度指令） | ✅ 完成 | c429410 |
| 5 | 问卷答案全部指令化（每个字段 → "如何对待该用户"的行为指令） | ✅ 完成 | c429410 |
| 6 | 意图聚焦：提示词开头 LLM 判断用户关键需求（模板非枷锁） | ✅ 完成 | 015f4fe |

## 二、今日已修复的 bug（✅）

| # | Bug | 根因 | 提交 |
|---|---|---|---|
| 1 | 停止按钮卡死（answer/method/knowledge 无停止机制） | 前端 3 模式缺 AbortController/signal/_showStopBtn | v0.42.3 |
| 2 | 上下文记忆遗忘（"她"→"妈妈"回指失败） | method/knowledge/affection 读 chat_hist 不写回 | v0.42.3 |
| 3 | 共享能力缺失（语言规范/检索 6 模式不对齐） | method 裸调/无 polish | v0.42.3 |
| 4 | 教学约束过强忽略用户诉求（"平均掌握度"+要公式被无视） | 引导式不剧透协议压制 + 缺系统术语机制 | 015f4fe |

## 三、已探索出方案待实施（⏳）

| # | 需求 | 方案 | 工作量 |
|---|---|---|---|
| 1 | 用户资料库/个人文件夹落本地（电脑端） | File System Access API（用户选真实文件夹+持久权限）+ OPFS 缓存层 + 后端同步 | M2 ~2 周 |
| 2 | 用户资料库落本地（手机端） | PWA + OPFS（全平台支持，主屏安装豁免 7 天清空） | M1 ~3 周 |
| 3 | 移动端 APP | PWA 先行 → Capacitor 封装 → Flutter 重写（memo 006 详述） | 1-8 周分阶段 |

## 四、Oracle 检视发现的待修项（⏳ T5 收尾）

### P0（v0.43 必做）
| # | 位置 | 问题 |
|---|---|---|
| P0-1 | server.py register | 注册后强制问卷（前端已做，后端 register 响应可加 flag）|
| P0-2 | infra/sessions.py | SESSIONS 加 threading.RLock（并发写 chat_hist race）|
| P0-3 | server.py teach_stream | 抽 _handle_teach_stream 到 services/（与同步路径对齐）|
| P0-4 | prompt_template.py | teach_stream 散落拼接迁到 _dyn_ctx + render_dynamic_slots |
| P0-5 | tests/ | 新增并发/6模式切换/长对话测试 |

### P1（强烈建议）
| # | 位置 | 问题 |
|---|---|---|
| P1-1 | server.py | teach_stream should_search 短输入短路（对齐 chat_stream）|
| P1-2 | services/handlers | problem/recommend/keyword_doc 接 user_library 槽 |
| P1-3 | paeg.py | SelfUpdateAgent 周期性调度触发 |
| P1-4 | services/ | 删 services/services/ 嵌套残留 |

## 五、同类错误探索结果（今日修复 4 类 bug 的同源变体遗漏）⭐

### ① 停止/中断机制（同类遗漏）
| # | 位置 | 问题 | 优先级 |
|---|---|---|---|
| 5-1 | index.html generateFile() L3196 | 文件生成只 btn.disabled，无 AbortController——无法中途停止 | P1 |
| 5-2 | index.html fetchResources() L4503 | 查资料无中断能力 | P1 |
| 5-3 | index.html generatePptFromResources() L4314 | PPT 生成无中断能力 | P1 |
| 5-4 | /api/solve（做题端点） | 无 SSE 中断流，用户只能等 | P2 |

### ② 记忆读写不对称（同类遗漏）
| # | 位置 | 问题 | 优先级 |
|---|---|---|---|
| 5-5 | routing.py _mode_auto_correct L64/80/88/97 | 模式纠正后**不写回 chat_hist**——连续纠正对话丢上下文 | **P0** |
| 5-6 | /api/solve | 完全不读不写 chat_hist（做题上下文丢失） | P1 |
| 5-7 | teach_stream 中途 abort | 用户中止后整段教学不写回 chat_hist | P1 |

### ③ 约束过强（同类遗漏）
| # | 位置 | 问题 | 优先级 |
|---|---|---|---|
| 5-8 | problem.py "请给完整解答" | 硬约束可能压制"只要思路/提示"的诉求 | P2 |

### ④ 共享能力不一致（同类遗漏）
| # | 位置 | 问题 | 优先级 |
|---|---|---|---|
| 5-9 | services/handlers/recommend.py | 无 polish/无 LANGUAGE_STYLE/无 chat_hist/无检索 | **P0** |
| 5-10 | services/handlers/problem.py | 无 polish/无 chat_hist（v0.43 问卷已接） | P1 |
| 5-11 | services/handlers/keyword_doc.py | 无 polish/无 chat_hist | P1 |

---

*本文档为 v0.43 收尾需求清单，随探索结果更新。*
