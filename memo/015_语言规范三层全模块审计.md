# 015 · 语言规范三层检测——全模块审计记录（v0.46）

> 日期：2026-08-11
> 触发：用户要求确认"每个模块对话都过了语言规范三层检测"（语言规范模块是核心亮点）。
> 结论：**8 个生成模块全部经过 L1+L2+L3 三层语言规范**，L0 保底层永不跳过。

---

## 一、三层架构确认

| 层 | 机制 | 位置 | 状态 |
|---|---|---|---|
| **L0 保底** | 11 条保底规则（含语言规范/格式/反AI腔/安全） | prompts.py `L0_RESERVED_RULES` | ✅ 永不跳过（L943 确认） |
| **L1 提示词约束** | 语法自检 7 项（主谓宾/动宾/介词/词法/句法） | prompts.py L83/L85/L100/L888 | ✅ 注入所有 system |
| **L2 规则检测** | `detect_ai_taste`（AI 味）+ `_check_ellipsis`（省略句） | services/polish.py → ai_taste_detector | ✅ 实测 ai_likelihood=0.4 |
| **L3 LLM 修正** | `refiner.refine` 重写（薇依语料矫正） | language_refiner.py | ✅ 实测触发重写 |

## 二、各模块 L2/L3 覆盖审计（8 模块全绿）

| 模块 | polish 调用点 | 状态 |
|---|---|---|
| teach 教学（流式） | refiner L1700 + polish L1706（双收口） | ✅ |
| teach 教学（同步） | refiner paeg.py L351 | ✅ |
| chat 闲聊 | server.py L3119 / L3523 | ✅ |
| answer 找答案 | server.py L3686 | ✅ |
| method 学习方法 | method.py L116 | ✅ |
| knowledge 知识库 | knowledge.py L144 | ✅ |
| affection 倾诉 | server.py 4 处（L2995 等）| ✅ |
| recommend 推荐 | recommend.py L76 | ✅ |
| resource summary | subagents.py L419（v0.46.2 新增）| ✅ |
| 早退分支（ppt/grade_blocked/unknown） | server.py（v0.46.2 补 polish）| ✅ |

## 三、实测验证证据（2026-08-11）

1. **L2 AI 味检测**：`detect_ai_taste('这是一段非常深刻且极具启发性的思考...')` → ai_likelihood=**0.4**（触发阈值 ≥0.4）
2. **三层 polish 输出**：`'同学们，我们来学习一下牛顿第二定律，它非常的重要。'` → 
   `'同学们，我们来学习牛顿第二定律。这条定律很重要，我们先把它的内容看清楚...'`（L3 重写生效）
3. **L0 保底**：11 条规则含语言规范，无掩码时也注入（`_build_constraint_layers(())` 验证）
4. **省略句检测**：`_check_ellipsis` 工作正常

## 四、审计方法

```powershell
# 全模块 polish 调用点
python audit_polish_all.py
# 三层实测
python verify_polish3.py
# L0 保底验证
python -c "from prompts import L0_RESERVED_RULES, _build_constraint_layers; print(len(L0_RESERVED_RULES))"
```

## 五、结论

**语言规范是 PAEG 的核心亮点，且实现完整**：
- L0/L1 保证"提示词层永远约束"
- L2/L3 保证"生成后规则检测 + LLM 修正"
- 8 个模块 × 所有生成分支全覆盖，无遗漏
- 新增生成点（resource summary）已自动纳入 polish（v0.46.2 起）
