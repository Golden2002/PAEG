# PAEG 亮点总览（v0.20 · 独立亮点文档）

> 本文档独立于《PAEG技术全景文档》——专门回答"这个项目强在哪、为什么是革命性的、
> 技术上怎么实现的、结果有多大转变"。面向对外宣传与技术理解双重用途。

---

## 一、一句话亮点

PAEG 不是又一个"给 LLM 套聊天框"的教育产品，而是**为教育重新设计了 Agent 架构**——
把教学的"过程"（诊断、计划、评估、调整、反思）从 LLM 的一次性输出中结构化地抽离出来，
让 Agent 真正**指挥** LLM 完成教育，而非**替代** LLM 回答问题。

---

## 二、八大亮点（含示例 + 技术说明 + 转变）

### 亮点 1：完整教学循环（不是聊天，是教学）

**示例转变**：
- 通用 AI 教育："什么是导数？→ 导数是函数在某一点的瞬时变化率……"（一次性回答）
- PAEG："先看一个现象：你骑自行车下坡，速度表上的数字在变。这个'此刻的快慢'就是导数要抓住的东西……（引导）你觉得'瞬时速度'这个概念矛盾在哪？"（六阶段教学）

**技术说明**：`paeg.teach()` 实现六阶段闭环——诊断（Diagnostor）→ 计划（Planner）→ 呈现（Presenter 循环）→ 评估（Evaluator）→ 调整（Adapter）→ 反思（自更新）。评估用确定性启发式（可复现不随机），LLM 只负责它最擅长的"讲解"。

**对 LLM 操控的提升**：把"教学"从 LLM 的一次性输出，变成 Agent 分步指挥的多轮流程——每一步 LLM 只做一件具体的事（诊断/讲解/评估），每步结果被 Agent 检查、反馈、修正。

### 亮点 2：子代理架构（LLM 只做擅长的事）

**示例**：同一个"什么是熵"，诊断子代理判断该讲多深，计划子代理选教学策略（高中物理用"墨水散开"直觉引入），呈现子代理生成讲解，评估子代理打分（0.4-0.95），调整子代理决定"换类比再讲一遍"还是"继续"。

**技术说明**：6+1 子代理（Diagnostor/Planner/Presenter/Evaluator/Adapter/AnswerSolver/AffectionSupportor）。设计原则：**诊断深度、评估分数、调整决策用确定性规则（可测试可复现），只有"生成讲解内容"这种真正需要 LLM 能力的地方才用 LLM**。

**对 LLM 操控的提升**：不用一个巨大 prompt 让 LLM 自己决定"该教多深、怎么教、讲没讲好"——每个环节由专门子代理承担，LLM 的注意力被精确引导到单一任务。

### 亮点 3：多层意图路由（Agent 自动判断该做什么）

**示例**：
- 用户设定"考研政治"，问"商品的价值由什么决定"→ Agent 自动识别是经济学，切换为经济学老师（Steering）
- 用户问"你今天怎么样"→ 意向性层识别非教学意图，走一般化回应（不再强行上课）
- 用户问"我最近好难过"→ 情绪拦截，走 affection 陪伴
- 用户问"这个界面的按钮是干嘛的"→ 自我指涉模块，返回界面指南

**技术说明**：server 层有多层拦截链：知识库 → 界面自指涉 → Steering（学科自动识别）→ 意向性层（LLM 判断教学意图）→ 情绪 → 学习方法 → 出题 → 元问题 → 教学。

**对 LLM 操控的提升**：**规则层（廉价确定性）优先，LLM 判断层兜底**——只有规则拦不住才花 token 让 LLM 判断，把 LLM 的"聪明"用在刀刃上。

### 亮点 4：自我进化（越用越懂怎么教）

**示例转变**：
- 通用 AI 教育：今天讲的和明天讲的没区别，不会记得上次哪里没讲好
- PAEG：成功教学后自动蒸馏知识点入库；教学效果差自动反思；周度任务分析失败案例生成改进建议；用户问"量子力学"（未收录学科）自动记录需求并反馈"后续优化升级"

**技术说明**：四路自进化——知识蒸馏（成功教学→evolved JSON→重启自动加载）、提示词补丁（SCOPE 双流）、工具经验、新学科需求闭环。质量门禁（Constitutional AI 风格）过滤有害/无效内容，不污染学习池。

**对 LLM 操控的提升**：Agent 的记忆与策略随对话进化，下一次教学自动注入"上次学到的改进"——LLM 的输出被持续校准。

### 亮点 5：MCP 双向打通（工具标准化）

**示例**：PAEG 内部 LLM 现在能通过 Function Calling 调用外部标准 MCP 工具（filesystem 读文件、memory 知识图谱）——`mcp__filesystem__list_directory` 返回真实目录列表。

**技术说明**：双方向——对外暴露教育工具（MCP Server，外部 agent 可连 :8765/mcp），对内连接外部标准工具（MCP Client，借鉴 oh-my-opencode 的 npx 标准 server）。

**对 LLM 操控的提升**：工具从封闭的 7 个扩到 34 个，LLM 可调用的"手"更多、更标准。

### 亮点 6：全局中文语言质量层（让输出像人话）

**示例转变**（这是用户最直观感受到的）：
- 之前："这句话本身，已经带着重量。"（动宾搭配不当）
- 之后："这句话本身已经很重。"（正确）
- 之前："不催你。"（无主语）→ 之后："我不急着反驳你。"（完整主谓）
- 之前："先不急。"（无主语）→ 之后："我们先不着急。"

**技术说明**：三层架构——
- L1 提示词约束（所有 system prompt 注入"句法骨架 + 合法省略边界 + 禁用句式"）
- L2 规则检测（零 LLM 成本，检测无主语短语/动宾搭配不当/AI 腔）
- L3 LLM 修正（minimal-edit，保持风格地补主语/修搭配）

**对 LLM 操控的提升**：LLM 输出的语法质量被**程序化保证**，不依赖 LLM 自觉——输出像"一位认真写中文的人"，而非"AI 生成物"。

### 亮点 7：affection 情绪支持（哲学三角）

**示例**：
- 用户："我好孤独，没什么朋友。"
- PAEG："我听见了。孤独这个词很轻，但你说出来的时候，它可能很重。我们不急着问你为什么孤独……这份孤独，在你身体里是什么感觉？是胸口发闷，还是像一层薄薄的雾，裹着你？"
- （现象学悬置 + 注意力陪伴 + 梅洛-庞蒂身体提问——不评判、不解决、不廉价安慰）

**技术说明**：AffectionSupportor 子代理加载 AffectionSAPAO.md（情绪支持宪法）——薇依（注意力/扎根）+ 尼采（自我克服/Amor Fati）+ 胡塞尔（悬置/回到事物本身）+ 生命现象学（约纳斯/梅洛-庞蒂/海德格尔）。语言风格参照约纳斯克制笔法（禁词清单 + 3 段参考）。

**对 LLM 操控的提升**：情绪场景不被"教学 harness"错误接管，LLM 被精确指挥为"以注意力陪伴"而非"给建议"。

### 亮点 8：博雅教育市场垂直（有灵魂的教育者）

**示例定位**：在"刷题 AI"的红海里，PAEG 是第一个以**完整教育人格（薇依式教师 Émile）+ 完整教学循环 + 情绪陪伴 + 自我进化**为壁垒的博雅教育垂直智能体。

**技术说明**：26 学科横跨文理（数学→哲学/美学/伦理/现象学）；薇依哲学内核（"爱是一种朝向"、"注意力是最稀有的慷慨"）；"先做人，再教书"最高原则；专属"批判性思维/公众表达/高效学习法"学科。

---

## 三、为什么这套架构是革命性的（六维度对比）

| 维度 | 通用 AI 教育 | PAEG |
|---|---|---|
| 教学循环 | 一次性问答 | 六阶段闭环（诊断→计划→呈现→评估→调整→反思）|
| 子代理分工 | 一个 prompt 干所有 | 6+1 子代理按"LLM 只做擅长事"拆分 |
| 意图路由 | 用户手动切模式 | 多层拦截链（Agent 自动判断）|
| 自我进化 | 静态知识 | 四路自进化（知识/提示词/工具/新学科需求闭环）|
| 工具互通 | 封闭工具 | MCP 双向打通（34 工具）|
| 语言质量 | 输出即答案 | 三层语言质量层（输出规范化）|

**一句话**：通用 AI 教育是"让 LLM 回答问题"，PAEG 是"**让 Agent 用教学法驱动 LLM 完成教育**"——这是从"工具"到"教师"的架构跃迁。

---

*本文档由 Sisyphus 编写，基于当前系统实际状态（v0.20）。详细技术实现见《PAEG技术全景文档》。*
