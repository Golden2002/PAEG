# 关机后恢复指南（3 步）

> 电脑关机/重启后，按下面顺序恢复：**先恢复 wbo（微信远程指挥）→ 再恢复 PAEG（教育者智能体）**。
> 总耗时约 1 分钟。写于 2026-08-07，适用于当前环境。

---

## 第 1 步：恢复 wbo（微信远程指挥）

wbo 负责"手机微信 ↔ 电脑 opencode"的桥接，恢复后才能远程指挥。

```powershell
# 打开 PowerShell，执行：
cd D:\wbo-workspace
wbo --daemon
```

**验证**：微信里给 ClawBot 发 `/status`，有回复即恢复成功。
（若提示 token 失效，先 `wbo stop`，再 `wbo --login` 重新扫码。）



> 经验：有时子进程占用监听端口，会导致手机端和电脑端都无法正常恢复——

 `wbo --login` 和 `wbo --daemon` 都会启动同一个 `opencode-ai serve` 服务（监听 4096 端口），如果后台已经有 daemon 在跑，再执行 `--login` 就会因端口占用而报错。

```bash
# 停止当前 daemon（如果存在）
wbo --stop

# 再强制清理所有 node 进程中涉及 opencode-ai 的（小心操作）
taskkill /F /IM node.exe /FI "WINDOWTITLE eq opencode-ai*"
# 检查 4096 端口是否被占用
netstat -ano | findstr :4096

# 杀掉 PID 为 23760 的进程及其子进程：(却做不到)
taskkill /PID 23760 /F
错误: 没有找到进程 "23760"。
taskkill /PID 6196 /F
成功: 已终止 PID 为 6196 的进程。

netstat -ano | findstr :4096

# 删除运行时生成的临时文件（不会影响配置）
Remove-Item -Recurse -Force C:\Users\团聚体\.wechat-bridge-opencode\*

Get-NetTCPConnection -LocalPort 4096
LocalAddress                        LocalPort RemoteAddress                       RemotePort State       AppliedSetting
------------                        --------- -------------                       ---------- -----       --------------
127.0.0.1                           4096      0.0.0.0                             0          Listen
```

`netstat` 显示 PID 23760 仍然在监听 4096 端口，但 `taskkill` 却告诉你找不到这个进程。

**Windows TCP 端口残留（zombie socket）** 问题：进程已经异常退出，但操作系统没有及时释放端口资源，导致端口被“卡住”了。

这种情况，仅靠 `taskkill` 或重启网络适配器有时并不能立刻解决，最稳妥的办法是**重启计算机**。但如果你不想重启，可以试试下面的强力清理方法：

1. 下载微软官方的 [TCPView](https://learn.microsoft.com/en-us/sysinternals/downloads/tcpview) 小工具。打开后找到监听 4096 端口的进程（即使显示为 PID 23760 或已消失），以管理员身份运行，右键选择 **“Close Connection”** 强制关闭所有关联的连接。然后再尝试启动 `wbo --login`。
   1. 如果仍然提示“拒绝访问”，说明系统内核完全锁死了这个端口，只能通过重启来释放。

[TCPView for Windows - Sysinternals | Microsoft Learn](https://learn.microsoft.com/en-us/sysinternals/downloads/tcpview)

打开一个**管理员权限**的 PowerShell，执行：

```powershell
netsh int ip reset
netsh winsock reset
```

执行完后，**必须重启计算机**才能生效（这其实又回到方案一）。



**成功登录**，登陆后在输入框

```bash
你好！
/session list
/session switch 5
```

注意！opencode通过**./config/opencode/opencode.json**——读取**./config/opencode/**目录下的或**C:\Users\团聚体\\.omo**目录下的json或jsonc（优先读取前一个目录）

目前

```bash
"C:\Users\团聚体\.omo\omo.jsonc" # 正在启用，模型接入minimax
"C:\Users\团聚体\.omo\oh-my-opencode.deepseek.json.bak" # 模型接入deepseek，但这是老的，没有ulw-loop skill
"C:\Users\团聚体\.omo\omo.deepseek.jsonc.bak"
```

**API:**

```bash
Minimax
sk-cp-LdaqNZIv956tiNsdcHZF1onTt9R2RtYG_eWta-HqfQ_pUy68VddkXfIHlByrujKJFM6Qxf9XU7fG-OPPx34brkf8Dz2BeTCXivrnbbLR2KWytrVtARGON0w

DeepSeek
REMOVED_DEEPSEEK_API_KEY
```

```bash
OpenCode 的配置加载优先级是：

项目级配置（最高优先级）：./opencode.json 或 ./.config/opencode/opencode.json（项目根目录下）

用户级全局配置：~/.config/opencode/opencode.json（即 C:\Users\团聚体\.config\opencode\opencode.json）

oh-my-openagent 专用配置：~/.omo/omo.json 或 ~/.omo/omo.jsonc（这是 OMO 插件的独立配置目录）


```



---

## 第 2 步：恢复 PAEG 教育者智能体（本地 + 公网）

(其实也可以让智能体来部署)

一键脚本已放在 `D:\wbo-workspace\`：

```powershell
# 双击或运行：
D:\wbo-workspace\start-paeg-public.ps1
```

**脚本自动完成**：
1. 释放 5000 端口（如有残留进程）
2. 后台启动 PAEG server（`05_实现原型\server.py`）
3. 验证 `/api/health`（应返回 200）
4. 启动 Cloudflare 隧道（前台，**窗口要一直开着**）

**拿到公网地址**：等 5-15 秒，窗口显示 `https://xxx.trycloudflare.com`，复制到任何设备浏览器即可访问（**手机访问需 Ctrl+F5 强刷**）。

> ⚠️ 隧道窗口关闭 = 公网断开。若需要，可改用固定域名方案（见技术文档 §6.3）。

---

## 第 3 步：验证

| 检查 | 命令/操作 | 期望 |
|---|---|---|
| PAEG 本地 | 浏览器开 `http://localhost:5000` | 页面正常 |
| PAEG 公网 | 浏览器开隧道 URL | 页面正常 |
| wbo | 微信发 `/status` | 有回复 |
| 数据 | 登录后左侧历史会话 | 记录还在（数据持久化在本地） |

---

## 常见问题

| 症状 | 原因 | 解决 |
|---|---|---|
| 5000 端口被占 | 旧进程残留 | 脚本已自动释放；或手动 `taskkill /F /PID <pid>` |
| 公网 URL 打不开 | 隧道窗口关了 / 隧道换了新 URL | 重新运行脚本，用**新的** URL |
| 微信没反应 | wbo 没起 / token 过期 | `wbo --daemon`；失效则 `wbo --login` |
| 页面功能"没生效" | 浏览器缓存旧 JS | **Ctrl+F5 强刷** |

---

*其他参考：wbo 完整手册见 `D:\wbo-workspace\README.md`；PAEG 部署细节见项目内《PAEG技术全景文档》§8（关机/断连恢复）。*
